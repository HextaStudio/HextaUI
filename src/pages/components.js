import { Layout } from "@/components/Layout";

const components = () => {
  return (
    <>
      <Layout docs>
        <main className="relative"></main>
      </Layout>
    </>
  );
};

export default components;
